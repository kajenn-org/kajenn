# Server application (`_server`) — open decisions

**Version**: 0.1 · **Last Updated**: 2026-09-17 · **Status**: 🔴 DA REVISIONARE

Source report: `kajenn-meta/verification/10_server/090_server-application.md`.

Rows of that report whose verdict is `DIVERGE` or `SILENT`, with the matching `Divergences` blocks. Both are verbatim from the report.

## Table

| # | Entry in decisions.md | Implementation (code file:line, test) | Docstring (file:line, text) | Handoff/finaldoc (file, date, text) | Transcript (uuid, date, role, text) | Verdict |
|---|---|---|---|---|---|---|
| 1 | Backlog (owner, 2026-08-21): "**Per-section auth tags in config** — each section names the tag that gates it; the monitor's gate is never weakened." | SILENT on a configuration path (`grep -rn "section_rules" src/ tests/`: no results). The rule is a literal in the code: `src/genro_asgi_server_app/server_sections/monitor_section.py:71` "MONITOR_RULE = \"SERVER_ADMIN\"", `:151` "@route(media_type=\"application/json\", auth_rule=MONITOR_RULE)", `src/genro_asgi_server_app/server_sections/users_section.py:93` "@route(auth_rule=\"SUPERADMIN\")" · `tests/server_app/test_server_monitor.py::TestMonitorGate::test_wrong_tags_are_forbidden` | SILENT | SILENT | `43531077-0c77-46fc-8686-e33fd2b8b413.jsonl` (2026-08-21, user) "mi pare che i tag siano arcodati. forse potrebbero far parte della configurazione ovvero in _server poterei dire i tag da usare per le vari ebntry" | SILENT |
| 2 | Backlog (owner, 2026-08-21): "**`_server/plugin_config` page** — the genro-routes entry-point tree made configurable from the admin surface." | SILENT. No section, route or page named `plugin_config` (`grep -rni "plugin_config" src/`: one line, other matter, `src/genro_asgi/plugins/openapi/translator.py:164` "openapi_config = metadata.get(\"plugin_config\", {}).get(\"openapi\", {})"). No test (`grep -rn "plugin_config" tests/`: no results) | SILENT | SILENT | `43531077-0c77-46fc-8686-e33fd2b8b413.jsonl` (2026-08-21, user) "tra l'altro sara necessaria una terza pagina in __server/plugin_config  che mi dia modo di settare i plugin usando il meccabnismo di genro routes. in parioca mi deve mostrare tutto l'albero degli entry point e darmi la paossobilita di configurarli" | SILENT |

## Divergences

### #1
- decisions.md says: "**Per-section auth tags in config** — each section names the tag that gates it; the monitor's gate is never weakened."
- the implementation does: SILENT on the decision. No code reads a per-section tag from the configuration; `grep -rn "section_rules" src/ tests/` has no results, and the gate is written in the decorator at each route: `src/genro_asgi_server_app/server_sections/monitor_section.py:71` "MONITOR_RULE = \"SERVER_ADMIN\"", `:151` "@route(media_type=\"application/json\", auth_rule=MONITOR_RULE)", `src/genro_asgi_server_app/server_sections/users_section.py:93` "@route(auth_rule=\"SUPERADMIN\")", `src/genro_asgi_server_app/server_sections/tasks_section.py:83` "@route(auth_rule=\"SUPERADMIN\")", `src/genro_asgi_server_app/server_sections/tokens_section.py:83` "@route(auth_rule=\"SUPERADMIN\")". The test that exercises the monitor gate pins the fixed tag: `tests/server_app/test_server_monitor.py::TestMonitorGate::test_wrong_tags_are_forbidden`, whose body is "server = make_server(Avatar(\"bob\", [\"SUPERADMIN\"]))" and "assert response_status(await http_request(server, path)) == 403".
- the docstring says: SILENT. No docstring in `src/` names section tags taken from the configuration. The rules are stated as fixed: `src/genro_asgi_server_app/server_sections/monitor_section.py:24` "Every route is gated ``auth_rule=\"SERVER_ADMIN\"``: the monitor exposes the whole server, so it is closed to anyone the operator has not admitted."; `src/genro_asgi_server_app/server_sections/users_section.py:19` "``auth_rule=\"SUPERADMIN\"`` — the section is ALWAYS declared (fixed structure,"; `src/genro_asgi_server_app/server_sections/tasks_section.py:27` "Every route is gated ``auth_rule=\"SUPERADMIN\"``."
- the handoff says: SILENT. No file in `temp/` or `.subtasks/` names per-section auth tags in configuration.
- the transcript `43531077-0c77-46fc-8686-e33fd2b8b413.jsonl` of 2026-08-21 says, user (16:58, uuid `1e7e49bf-0720-4095-8b64-88410cf4fa26`): "console forse è il migliore: tra l'altro mi pare che i tag siano arcodati. forse potrebbero far parte della configurazione ovvero in _server poterei dire i tag da usare per le vari ebntry". Assistant (16:58, uuid `30bc4cb0-6c74-4887-91d9-1eab3e301d01`): "un kwarg in più tipo `section_rules = {\"monitor\": ..., \"tasks\": ...}` con gli attuali come default, e le route che leggono la regola dalla sezione invece che dal decoratore. La decisione di stamattina impone solo che il gate del monitor non si indebolisca: config che sceglie *quale* tag, mai che lo toglie." Confirmed by the user in the following turn (17:00, uuid `5f59ecae-39d0-40ec-8cf6-0d60a10ea9ab`): "si ma tu annota comnque n: fa parte dell0interfaccia amministrativa del server." and then (17:02, uuid `723298f4-9c3d-4ceb-82c3-e238df2f8feb`): "si".

### #2
- decisions.md says: "**`_server/plugin_config` page** — the genro-routes entry-point tree made configurable from the admin surface."
- the implementation does: SILENT on the decision. No section, route or page named `plugin_config` exists. `grep -rni "plugin_config" src/` finds one line, which is other matter: `src/genro_asgi/plugins/openapi/translator.py:164` "openapi_config = metadata.get(\"plugin_config\", {}).get(\"openapi\", {})". The sections the server application declares are the ones listed at `src/genro_asgi_server_app/server_sections/__init__.py:20-24` "the login methods), ``UsersSection`` (SUPERADMIN-gated user management at" … "``monitor``, gated SERVER_ADMIN)." No test names it: `grep -rn "plugin_config" tests/` has no results.
- the docstring says: SILENT. The only occurrence of the name in `src/` is the openapi translator line above.
- the handoff says: SILENT. The only occurrence in `temp/` is `temp/issue_storage_swap.md` (2026-07-30), line 104: "| plugin config (storage_mixin consumers) | `secure:plugin_config.json` | `site:plugin_config.json`, `encrypted=True` |" — that is the storage path, not the admin page.
- the transcript `43531077-0c77-46fc-8686-e33fd2b8b413.jsonl` of 2026-08-21 says, user (17:00, uuid `5f59ecae-39d0-40ec-8cf6-0d60a10ea9ab`): "tra l'altro sara necessaria una terza pagina in __server/plugin_config  che mi dia modo di settare i plugin usando il meccabnismo di genro routes. in parioca mi deve mostrare tutto l'albero degli entry point e darmi la paossobilita di configurarli". Assistant (17:03, uuid `22276f13-d9a8-440f-99eb-e4dd11376e9f`), after the user's "si": "Creati su genropy/genro-asgi (verificato: è l'org giusta): - [#30 — Configurable auth tags for _server sections](https://github.com/genropy/genro-asgi/issues/30) - [#31 — Admin page _server/plugin_config for genro-routes plugins](https://github.com/genropy/genro-asgi/issues/31)". Taken up again in the transcript `f54e2223-44b5-4008-876c-657ecf667955.jsonl` of 2026-09-02, assistant: "| [#31](https://github.com/genropy/genro-asgi/issues/31) | Admin page _server/plugin_config for genro-routes plugins | enhancement | 08-21 | | [#30](https://github.com/genropy/genro-asgi/issues/30) | Configurable auth tags for _server sections | enhancement | 08-21 |".

