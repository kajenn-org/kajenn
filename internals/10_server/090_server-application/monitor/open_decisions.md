# Monitor — open decisions

**Version**: 0.1 · **Last Updated**: 2026-09-17 · **Status**: 🔴 DA REVISIONARE

Source report: `kajenn-meta/verification/10_server/090_server-application__monitor.md`.

Rows of that report whose verdict is `DIVERGE` or `SILENT`, with the matching `Divergences` blocks. Both are verbatim from the report.

## Table

| # | Entry in decisions.md | Implementation (code file:line, test) | Docstring (file:line, text) | Handoff/finaldoc (file, date, text) | Transcript (uuid, date, role, text) | Verdict |
|---|---|---|---|---|---|---|
| 1 | friction: "Pool monitor parity with the pre_refactoring panels is NOT built yet (second pass), Prometheus metrics missing." | SILENT. No code declares the absence. No Prometheus surface exists (`grep -rni "prometheus" src/ tests/`: no results); the monitor serves three routes and nothing else, `src/genro_asgi_server_app/server_sections/monitor_section.py:151` "@route(media_type=\"application/json\", auth_rule=MONITOR_RULE)" (`snapshot`), `:164` (`panels`), `:187` "@route(media_type=\"text/javascript\", auth_rule=MONITOR_RULE)" (`panel`) · tests cover those three and no parity or metrics claim: `tests/server_app/test_server_monitor.py::TestSnapshot`, `::TestPanels`, `::TestPanelModule` | SILENT | `temp/piano_accensione_genropy_2026-08-19.md` (2026-08-19) line 103 "6. Osservabilità: pannelli del monitor, metriche Prometheus."; `temp/design_orchestrazione_v3_2026-08-16.md` (2026-08-16) line 719 "### 13.2 `_server/prometheus` (requisito del titolare, 2026-08-16)" | `112007f3-8670-4a4b-87f9-640d4ca407f6.jsonl` (2026-09-08, assistant) "Non costruito: … parità del monitor del pool e metriche Prometheus" | SILENT |

## Divergences

### #1
- decisions.md says: "Pool monitor parity with the pre_refactoring panels is NOT built yet (second pass), Prometheus metrics missing."
- the implementation does: SILENT. `grep -rni "prometheus" src/ tests/` has no results, so no Prometheus exposition surface exists and no test asks for one. What the section does build is the three routes of the monitor: `src/genro_asgi_server_app/server_sections/monitor_section.py:151` "@route(media_type=\"application/json\", auth_rule=MONITOR_RULE)" over `snapshot`, `:161` "apps = {key: item.app_snapshot for key, item in self.monitor_contributors.items()}", `:164` over `panels`, `:181` "descriptor = dict(item.app_panel)", `:187` "@route(media_type=\"text/javascript\", auth_rule=MONITOR_RULE)" over `panel`. The tests exercise those three and claim no parity: `tests/server_app/test_server_monitor.py::TestSnapshot::test_one_entry_per_application`, `::TestPanels::test_panels_and_snapshot_agree`, `::TestPanelModule::test_the_module_is_served_as_javascript`. No code states that something is missing: a codebase describes what exists.
- the docstring says: SILENT. No docstring names parity with the `pre_refactoring` panels; the only occurrence of the word in `src/` is other matter, `src/genro_asgi_multiworker_spa/orchestration/__init__.py:17` "This subpackage is the machine running today: the pre_refactoring commander and". The monitor's docstring describes only what is there: `src/genro_asgi_server_app/server_sections/monitor_section.py:15` "The ``_server/monitor`` section: the live view of the running server." and `:24` "Every route is gated ``auth_rule=\"SERVER_ADMIN\"``".
- the handoff says: `temp/piano_accensione_genropy_2026-08-19.md` (2026-08-19), in the list of what remains to be done, line 103: "6. Osservabilità: pannelli del monitor, metriche Prometheus." Same list, line 101: "4. Trasloco a caldo, `recycle_worker`, `hard_restart`, `dump`/`restore`." `temp/handoff_m5_2026-08-19.md` (2026-08-19) line 82: "osservabilità (pannelli del monitor, metriche); e la meccanica che oggi vive solo nel legacy". `temp/design_orchestrazione_v3_2026-08-16.md` (2026-08-16) line 719: "### 13.2 `_server/prometheus` (requisito del titolare, 2026-08-16)", line 721: "Una section sotto `_server` che risponde in formato di esposizione Prometheus (testo, `# HELP`/`# TYPE`)."
- the transcript `22376a24-d12f-46c3-a8af-9414f78bc4f7.jsonl` of 2026-08-13 says, user (09:00): "inoltre vorrei vedere di intr9odurre se gi non c'è il discorso prometheus ai 3 livelli". Assistant in the same file (09:01): "Prometheus invece non c'è affatto: lo introduco come design ai tre livelli." The transcript `cdf777f6-0db8-40ec-b07a-ec18ffe2eac0.jsonl` of 2026-08-21, assistant (20:32): "Il residuo invece è quasi tutto assente dal core nuovo — `hard_restart`, `recycle_worker`, `build_plan`/`execute_plan`, `prometheus`, `USER_PENDING_MAX_ITEMS`, `notify_user`, il worker in-process: zero file in `spa/orchestration/`, tutti presenti nel pre_refactoring." The transcript `112007f3-8670-4a4b-87f9-640d4ca407f6.jsonl` of 2026-09-08, assistant (16:56): "Non costruito: comando di reboot deliberato su `_server`, reboot di un solo gruppo, classificatore dei path per il reload watcher, parità del monitor del pool e metriche Prometheus, worker in-process".

