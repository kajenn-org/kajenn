# Inspector — open decisions

**Version**: 0.1 · **Last Updated**: 2026-09-17 · **Status**: 🔴 DA REVISIONARE

Source report: `kajenn-meta/verification/10_server/090_server-application__inspector.md`.

Rows of that report whose verdict is `DIVERGE` or `SILENT`, with the matching `Divergences` blocks. Both are verbatim from the report.

## Table

| # | Entry in decisions.md | Implementation (code file:line, test) | Docstring (file:line, text) | Handoff/finaldoc (file, date, text) | Transcript (uuid, date, role, text) | Verdict |
|---|---|---|---|---|---|---|
| 1 | "Owner direction (2026-08-22): the inspector starts by showing ONLY the server's own structures; every application can be CALLED to contribute its own inspect panel — the same contribution-contract style the monitor already uses (`app_snapshot`/`app_panel`/`panel_source`). The SPA enrichment lives in the SPA application, never in the section." | The section shows one pool and calls no application: `src/genro_asgi_multiworker_spa/inspector_section.py:88` "return {self.application.code: await self.application.commander.get_pool_census()}". No `app_snapshot`/`app_panel`/`panel_source` call anywhere in the inspector; the contract is read only by the monitor, `src/genro_asgi_server_app/server_sections/monitor_section.py:161` "apps = {key: item.app_snapshot for key, item in self.monitor_contributors.items()}", `:181` "descriptor = dict(item.app_panel)" · `tests/spa/test_inspector_section.py::test_the_census_is_json_under_the_front_code` asserts "set(census) == {\"site\"}" | SILENT on the contribution contract. The section's docstring names three addresses and one pool: `src/genro_asgi_multiworker_spa/inspector_section.py:15` "The ``_server/inspector`` section: the SPA pool shown to a human." | SILENT | `f2d6a636-3346-46a5-910a-f1e3d3f113bf.jsonl` (2026-08-22, user) "potremmo ad esempio immaginare che l'inspecto mostr inizialmente strutture solo del server e che ogni applicazione abbia la possibilita di farsi chiamare per fornitre il suo pannellino di inspect" | DIVERGE |
| 2 | "Owner decision (2026-09-07, workflow `core-spa-boundary` phase 3): the section itself left the server sections. It lives in `genro_asgi_multiworker_spa/inspector_section.py`, its parent is the `SpaApplication` that attaches it on startup under `GNR_ASGI_INSPECTOR`, and it reads that front's own pool through `self.application.commander`. A server has ONE orchestrated application, so a second attach is a `FatalBootError`." | `src/genro_asgi_multiworker_spa/inspector_section.py:69-70` "def __init__(self, application: SpaApplication) -> None:\n        self.application = application"; `:88` "return {self.application.code: await self.application.commander.get_pool_census()}"; `src/genro_asgi_multiworker_spa/spa_app.py:814` "if not os.environ.get(INSPECTOR_ENV_VAR):", `:825-829` "if \"inspector\" in server_app.sections:\n            self.refuse_boot(…)", `:830` "server_app.attach_section(InspectorSection(self), name=\"inspector\")" · `tests/spa/test_inspector_section.py::test_a_second_front_on_one_server_does_not_boot` "with pytest.raises(FatalBootError, match=\"already attached\")" and "assert server_app.sections[\"inspector\"].application is front" | `src/genro_asgi_multiworker_spa/inspector_section.py:35-37` "Parent (dual relationship): the SpaApplication that attached it, stored as ``self.application``. A server has ONE (owner, 2026-09-07), so the census is keyed by that front's code"; `src/genro_asgi_multiworker_spa/spa_app.py:806` "A server has ONE orchestrated application (owner, 2026-09-07), so a mount already there means a second front is booting on the same server"; `:812` "FatalBootError: ``inspector`` is already attached." | `temp/piano_confine_core_spa_v0.2_2026-09-08.md` (2026-09-08) "calls `attach_section(InspectorSection(server_app), name=\"inspector\")` unless `\"inspector\"` is already in `server_app.sections` (a second front finds it attached: legitimate state, not a guard)"; `temp/cronologia_2026-08-23_2026-09-12.md` (2026-09-12) "(D) un server ha UNA applicazione orchestrata; una seconda attach è `FatalBootError`." | `971252ca-0607-4e3b-836d-e5d51e9a407b.jsonl` (2026-09-07, assistant) "**un secondo attach è `FatalBootError`**", confirmed in the following turn by the same file (2026-09-07 08:54:38Z, uuid `7a3c3282-b720-4ab4-8b8b-46a4741cd55f`, user) "ok entrambe" | DIVERGE |
| 3 | friction, struck through: "~~The current implementation contradicts the contribution contract: `inspector_section.py:48` imports `SpaApplication` and `isinstance`-checks the mounted apps — SPA knowledge hardwired into a server section.~~ Settled 2026-09-07: the section moved to the SPA world, the `isinstance` loop is gone, and `SpaApplication` is named under `TYPE_CHECKING` only." | The file lives in the SPA package and holds no `isinstance`: `grep -n "isinstance\|spa_fronts" src/genro_asgi_multiworker_spa/inspector_section.py` has no results; `src/genro_asgi_multiworker_spa/inspector_section.py:52-53` "if TYPE_CHECKING:\n    from .spa_app import SpaApplication" · `tests/spa/test_inspector_section.py:38` "from genro_asgi_multiworker_spa.inspector_section import INSPECTOR_ENV_VAR" | `src/genro_asgi_multiworker_spa/inspector_section.py:26-28` "It lives here, in the SPA world, and not among the server sections, because it reads a pool and nothing else — a core that carries no orchestration must not import one to look at it." | SILENT | `971252ca-0607-4e3b-836d-e5d51e9a407b.jsonl` (2026-09-07, assistant) "`SpaApplication` in `inspector_section.py` solo sotto `TYPE_CHECKING`. L'eccezione alla regola 3 (import dentro il metodo) **non concessa**.", confirmed by the same file (2026-09-07 08:54:38Z, uuid `7a3c3282-b720-4ab4-8b8b-46a4741cd55f`, user) "ok entrambe" | SILENT |

## Divergences

### #1
- decisions.md says: "the inspector starts by showing ONLY the server's own structures; every application can be CALLED to contribute its own inspect panel — the same contribution-contract style the monitor already uses (`app_snapshot`/`app_panel`/`panel_source`). The SPA enrichment lives in the SPA application, never in the section."
- the implementation at `src/genro_asgi_multiworker_spa/inspector_section.py:88` does something else: "return {self.application.code: await self.application.commander.get_pool_census()}" — one front's pool, keyed by that front's code, and no application is called. The three addresses the section serves are `page`, `census` and `stream` (`:73`, `:82`, `:91`), none of which collects a contributed panel. The contribution contract is read by the monitor alone: `src/genro_asgi_server_app/server_sections/monitor_section.py:110-111` "and hasattr(section, \"app_snapshot\")\n            and hasattr(section, \"app_panel\")", `:161` "apps = {key: item.app_snapshot for key, item in self.monitor_contributors.items()}", `:200` "source = getattr(item, \"panel_source\", None)"; the two properties are declared once, at `src/genro_asgi/application.py:230` "def app_snapshot(self) -> dict[str, Any]:" and `:240` "def app_panel(self) -> dict[str, Any]:". The test pins the census to the single front: `tests/spa/test_inspector_section.py::test_the_census_is_json_under_the_front_code` "assert set(census) == {\"site\"}".
- the docstring says: SILENT on the contract. No docstring in `src/` names an inspect panel contributed by the applications. The one cited contract exists for the monitor only: `src/genro_asgi_server_app/server_sections/monitor_section.py:28-30` "What an app IS at this instant comes from its ``app_snapshot`` (polled, aggregated here under ``apps``); WHO draws it comes from its ``app_panel``" and `:35-37` "``panel_source`` hands over the ES module as text, and ``panels`` fills the descriptor's ``src`` with this section's ``panel`` route". The inspector's own docstring describes three addresses and one pool: `src/genro_asgi_multiworker_spa/inspector_section.py:15` "The ``_server/inspector`` section: the SPA pool shown to a human."
- the handoff says: SILENT. No file in `temp/` or `.subtasks/` names the contribution contract applied to the inspector.
- the transcript `f2d6a636-3346-46a5-910a-f1e3d3f113bf.jsonl` of 2026-08-22 says, user (14:29, uuid `d1dac1e8-f697-444d-9a82-63bcfaf3ef92`): "possiamo anche arricchire degli arcomebti iniziate in server per arr9icchirli in spa. potremmo ad esempio immaginare che l'inspecto mostr inizialmente strutture solo del server e che ogni applicazione abbia la possibilita di farsi chiamare per fornitre il suo pannellino di inspect". Assistant (14:30, uuid `85ee30cc-e0d3-451f-a7ea-8f8bc9fcb677`): "il seme di questa idea esiste già nel codice: è esattamente il contratto del **monitor** (`app_snapshot` / `app_panel` / `panel_source` su `BaseApplication` ... Il tuo disegno estende lo stesso pattern all'inspector: **base server = solo le strutture del server; ogni applicazione contribuisce il proprio pannellino di inspect**". Confirmed by the user (14:34, uuid `28039058-4007-414c-aa49-3f00f53dfa2f`): "ok procedi".
- `decisions.md` itself carries, after the 2026-09-07 decision, the paragraph "**This settles the friction below, and not by the contribution contract the 2026-08-22 direction imagined.**" and "The direction stands for the sections that DO stay in the core: the monitor is the one that uses it." The paragraph is recorded here and is not used to resolve the row.

### #2
- decisions.md says: "It lives in `genro_asgi_multiworker_spa/inspector_section.py`, its parent is the `SpaApplication` that attaches it on startup under `GNR_ASGI_INSPECTOR`, and it reads that front's own pool through `self.application.commander`. A server has ONE orchestrated application, so a second attach is a `FatalBootError`."
- the implementation does the same. The file is `src/genro_asgi_multiworker_spa/inspector_section.py`; the parent is the front: `:69-70` "def __init__(self, application: SpaApplication) -> None:\n        self.application = application"; the pool is read through the commander: `:88` "return {self.application.code: await self.application.commander.get_pool_census()}" and `:109` "commander = self.application.commander"; the environment variable gates the mount: `:58` "INSPECTOR_ENV_VAR = \"GNR_ASGI_INSPECTOR\"" and `src/genro_asgi_multiworker_spa/spa_app.py:814` "if not os.environ.get(INSPECTOR_ENV_VAR):"; the second attach refuses the boot: `src/genro_asgi_multiworker_spa/spa_app.py:825-829` "if \"inspector\" in server_app.sections:\n            self.refuse_boot(\n                \"the 'inspector' section is already attached, so a second front is \"\n                \"booting on this server and a server has one orchestrated application\"\n            )", with `refuse_boot` raising at `:779` "raise FatalBootError(failure) from cause". The attach passes the front itself: `:830` "server_app.attach_section(InspectorSection(self), name=\"inspector\")". Test: `tests/spa/test_inspector_section.py::test_a_second_front_on_one_server_does_not_boot` "with pytest.raises(FatalBootError, match=\"already attached\")" and "assert server_app.sections[\"inspector\"].application is front".
- the docstring says the same: `src/genro_asgi_multiworker_spa/inspector_section.py:35-37` "Parent (dual relationship): the SpaApplication that attached it, stored as ``self.application``. A server has ONE (owner, 2026-09-07), so the census is keyed by that front's code and carries the one entry the page already reads."; `:23-25` "**Mounting IS the gate.** The SPA front attaches this section on its own startup, only when ``GNR_ASGI_INSPECTOR`` is set"; `src/genro_asgi_multiworker_spa/spa_app.py:806-810` "A server has ONE orchestrated application (owner, 2026-09-07), so a mount already there means a second front is booting on the same server — the state that decision declares impossible, and the boot says so instead of watching one pool of two."; `:811-812` "Raises:\n            FatalBootError: ``inspector`` is already attached."
- the handoff of 2026-09-08 says the opposite on two points. `temp/piano_confine_core_spa_v0.2_2026-09-08.md`, phase 3, field `Decisions:` (line 45): "it finds the mounted `ServerApplication` in `self.server.applications` and calls `attach_section(InspectorSection(server_app), name=\"inspector\")` unless `\"inspector\"` is already in `server_app.sections` (a second front finds it attached: legitimate state, not a guard)". Same file, field `Files:` (line 44): "`src/genro_asgi/applications/server_sections/inspector_section.py` → `src/genro_asgi/spa/inspector_section.py`". The parent there is the `ServerApplication` and not the `SpaApplication`, the second attach is "legitimate state, not a guard" and not a `FatalBootError`, and the destination is `src/genro_asgi/spa/` and not `genro_asgi_multiworker_spa/`.
- a second, later handoff says what `decisions.md` says. `temp/cronologia_2026-08-23_2026-09-12.md` (2026-09-12), line 77: "(D) un server ha UNA applicazione orchestrata; una seconda attach è `FatalBootError`."; same file, section "Package proprio (#73, 09-07, core 0.44 / bridge PR #14)": "`genro_asgi_multiworker_spa/` è un secondo top-level package nella stessa distribuzione; il core non lo importa."
- the transcript `971252ca-0607-4e3b-836d-e5d51e9a407b.jsonl` of 2026-09-07 says, assistant (08:48, uuid `327ea667-536d-445a-984d-ae6d22da5794`): "`InspectorSection(self)` dal front, e la sezione legge l'unico pool con `self.application.commander`. `InspectorSection.spa_fronts` e il suo `isinstance` **spariscono**. ... **un secondo attach è `FatalBootError`**: `\"inspector\"` già in `server_app.sections` significa una seconda applicazione orchestrata sullo stesso server, lo stato che il titolare ha dichiarato impossibile." Confirmed by the user in the following turn (2026-09-07 08:54:38.694Z, uuid `7a3c3282-b720-4ab4-8b8b-46a4741cd55f`, role user, whole text of the record): "ok entrambe", in answer to the assistant's explicit request (08:48, uuid `d480fe8e-8066-4514-9554-96949b77a5cf`): "La forma decisa dal titolare nella chat del foreman, che ti ho riassunto sopra — te la mostro perché il protocollo vuole la tua conferma prima che io tocchi il codice."

### #3
- decisions.md says: "Settled 2026-09-07: the section moved to the SPA world, the `isinstance` loop is gone, and `SpaApplication` is named under `TYPE_CHECKING` only."
- the implementation does the same. The file is `src/genro_asgi_multiworker_spa/inspector_section.py`, inside the SPA package; `grep -n "isinstance\|spa_fronts" src/genro_asgi_multiworker_spa/inspector_section.py` has no results, so neither the loop nor the method survives; the name is imported under the guard only: `:52-53` "if TYPE_CHECKING:\n    from .spa_app import SpaApplication", declared at `:45` "from typing import TYPE_CHECKING, Any". The test suite reaches the section at its new address: `tests/spa/test_inspector_section.py:38` "from genro_asgi_multiworker_spa.inspector_section import INSPECTOR_ENV_VAR".
- the docstring says: `src/genro_asgi_multiworker_spa/inspector_section.py:26-28` "It lives here, in the SPA world, and not among the server sections, because it reads a pool and nothing else — a core that carries no orchestration must not import one to look at it."
- the handoff says: SILENT. No file in `temp/` or `.subtasks/` records the closing of the friction. The `TYPE_CHECKING` search over `temp/` and `.subtasks/` finds one line, of other matter: `temp/piano_mcp_method_route_2026-09-05.md:78` "sotto `TYPE_CHECKING`; genro-routes è dipendenza dura, già importata così in `spa_worker.py:261`". The `spa_fronts` search has no results.
- the transcript `971252ca-0607-4e3b-836d-e5d51e9a407b.jsonl` of 2026-09-07 says, assistant (08:48, uuid `327ea667-536d-445a-984d-ae6d22da5794`): "`SpaApplication` in `inspector_section.py` solo sotto `TYPE_CHECKING`. L'eccezione alla regola 3 (import dentro il metodo) **non concessa**." Confirmed by the user (2026-09-07 08:54:38.694Z, uuid `7a3c3282-b720-4ab4-8b8b-46a4741cd55f`, role user, whole text of the record): "ok entrambe". Assistant (08:59, uuid `8bb74bc3-d956-430f-9440-06723efae78c`): "nella `decisions.md` dell'inspector ho registrato che l'attrito si chiude **contro** la direzione del 2026-08-22".

