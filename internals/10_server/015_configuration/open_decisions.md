# Configuration — open decisions

**Version**: 0.1 · **Last Updated**: 2026-09-17 · **Status**: 🔴 DA REVISIONARE

Source report: `kajenn-meta/verification/10_server/015_configuration.md`.

Rows of that report whose verdict is `DIVERGE` or `SILENT`, with the matching `Divergences` blocks. Both are verbatim from the report.

## Table

| # | Entry in decisions.md | Implementation (code file:line, test) | Docstring (file:line, text) | Handoff/finaldoc (file, date, text) | Transcript (uuid, date, role, text) | Verdict |
|---|---|---|---|---|---|---|
| 1 | §1, D15: "One configuration is the whole site; each process materializes the projection of its own role." | SILENT (no code in `src/genro_asgi/config/` projects one description per process role; `src/genro_asgi/asgi_server.py:127` "self._config = self._build_config(config, kwargs)" builds one handler per server over the whole description) | SILENT (no docstring of `src/genro_asgi/config/` speaks of a per-process-role projection) | SILENT | SILENT | SILENT |
| 2 | §1, ratified 2026-07-29: "Nothing materializes a component from the outside: **the class that needs the values reads them.** A value passed explicitly at construction wins over the configured one, **wholesale per value**" | `src/genro_asgi/asgi_server.py:128` "kwargs = {**self._configured_kwargs(self.config), **kwargs}" — the explicit kwargs are folded last, one kwarg at a time · `tests/core/test_config.py::test_an_explicit_kwarg_wins_over_the_configured_one`, `tests/core/test_config.py::test_a_server_composed_in_code_has_one_too` | `src/genro_asgi/asgi_server.py:30-32` "Nothing materializes a server from the outside; the class that needs the values reads them. Explicitly passed kwargs WIN over the configured ones, wholesale per kwarg"; `src/genro_asgi/config/handler.py:20-22` "It NEVER builds a server: the server builds ITS OWN handler (``AsgiServer(config=source)``) and asks these helpers for its kwargs, so there is one direction of dependency and no materializer." | `.subtasks/config-always/finaldoc.md` (2026-09-12) "`AsgiServer._build_config` always returns a `ConfigurationHandler`; `server.config` is typed `ConfigurationHandler`, never `None`" | SILENT | SILENT |
| 3 | §1: "there is no builder, no factory, no assembler that knows the whole system. There is a description, and each part reads its own part of it." | `src/genro_asgi/asgi_server.py:175-196` `_configured_kwargs` calls one read-door helper per section and hands each kwarg to the class that peels it; nothing in `src/genro_asgi/config/` constructs a component · `tests/core/test_config.py::test_the_handler_stays_reachable_as_the_read_door`, `tests/core/test_config_env.py::test_each_app_reads_only_its_own_prefix` | `src/genro_asgi/config/handler.py:20-22` "there is one direction of dependency and no materializer." | SILENT | SILENT | SILENT |
| 4 | §2, ratified 2026-07-29: "A description is a **class with one method**, calling a grammar to build a document. Being executed rather than parsed is what makes it possible to hand over **objects** — an application class is imported and passed as itself" | `src/genro_asgi/config/builder.py:60` "class AsgiConfigBuilder(ConfigBuilder, AsgiServerGrammar):" with `main(self, root)` as the one method a recipe overrides; the class travels as an object, `src/genro_asgi/config/elements.py:358-360` "def application(self, app_class: type," takes the imported class itself · `tests/core/test_config.py::test_a_recipe_instance_is_accepted`, `tests/core/test_default_configuration.py::test_an_instance_is_refused_by_the_grammar`, `tests/core/test_config.py::test_application_without_app_class_rejected_by_grammar` | `src/genro_asgi/config/builder.py:18-21` "A site subclasses it in a ``config.py`` and overrides ``main(self, root)``; the runtime reads the built tree through ``ConfigurationHandler`` and nothing else."; `src/genro_asgi/config/elements.py:58-59` "A recipe subclasses ``AsgiConfigBuilder`` and overrides ``main(self, root)``; application classes are imported and passed as objects" | `.subtasks/config-always/finaldoc.md` (2026-09-12) "`applications=` takes CLASSES, or `(class, params)` pairs; an instance is refused by the grammar itself (`Validation failed: 'app_class': expected type`), pinned by a test" | SILENT | SILENT |
| 5 | §2, owner 2026-08-23: "A description too long to read at a glance is split into methods, one per section, each taking the parent node. The recipe orchestrates; the sections describe." | `src/genro_asgi/config/builder.py:144-149` "def main(self, root: Any) -> None: … cfg = root.configuration() / self.site_section(cfg) / self.server_section(cfg) / self.storage_section(cfg)" — the shipped recipe is itself written that way, each section a method taking the parent node · `tests/test_documentation_recipes.py::test_complete_configuration_constructs`, `tests/core/test_config.py::test_server_section_reaches_the_serve_defaults` | `src/genro_asgi/config/builder.py:23-24` "Recipes orchestrate in ``main`` and delegate each section to a method taking the PARENT node, so a section stays small enough to read at a glance"; `src/genro_asgi/config/elements.py:33-35` "the recipe orchestrates in ``main`` and delegates each section to a method taking the PARENT node." | SILENT | SILENT | SILENT |
| 6 | §2: "**Secrets never appear in a recipe.** … Words whose only legitimate source is outside **refuse a literal at the recipe line**: the bootstrap administrator password cannot be typed into a description at all." | No word of the grammar refuses a literal: every secret-bearing attribute is annotated `<type> \| BagResolver` and takes either, `src/genro_asgi/config/elements.py:286` "password: str \| BagResolver = None", `src/genro_asgi/config/elements.py:318` "def storage(self, app: type, storage_key: str \| BagResolver = None) -> None:". The bootstrap password word was removed rather than made refusing · `tests/core/test_config.py::test_the_bootstrap_password_is_no_longer_a_word`; `tests/core/test_config_env.py::test_basic_user_password_attribute_resolves_into_the_auth_entries` passes a resolver, and no test asserts a literal is refused | `src/genro_asgi/config/elements.py:30-32` "an attribute whose value may come from outside (env, file, url) is annotated ``<type> \| BagResolver`` and receives the resolver IN PLACE"; `src/genro_asgi/config/elements.py:244-245` "The server creates no user either (owner, 2026-09-12): there is no bootstrap password word." | `.subtasks/config-always/progress.md` (2026-09-12) "**`admin_password` cannot be written into the tree.** The grammar element `authentication.admin_password(node_value: BagResolver = None)` REFUSES a literal by a ratified decision (\"secrets stay out of recipes\"…)"; `.subtasks/config-always/finaldoc.md` (2026-09-12) "`ddd7d2c` … `feat: the server creates no user, and the session snapshot is a configuration word`" | SILENT | DIVERGE |
| 7 | §3, owner 2026-08-23: "There is no central vocabulary. **Each part declares the words it consumes, next to the code that reads them**, and the description is validated against the union of those declarations." | `src/genro_asgi/config/elements.py:357` "@element(parent_tags=\"applications\", _meta={\"subbuilder\": \"app_class:grammar\"})", so the app's own grammar governs its children; `src/genro_asgi/config/elements.py:317` the `storage` element carries `_meta={"subbuilder": "app:grammar"}` for genro-storage's · `tests/core/test_config.py::test_the_apps_own_subtree_is_read_through_the_handler`, `tests/core/test_config.py::test_an_undeclared_child_of_the_mounted_grammar_raises`, `tests/core/test_config.py::test_a_mount_without_base_path_is_rejected_by_the_foreign_grammar` | `src/genro_asgi/application.py:52-56` "Every application also carries its own CONFIGURATION GRAMMAR as the class attribute ``grammar`` … the site recipe mounts it at the ``application(app_class=...)`` line (subbuilder by reference), so the app declares its own vocabulary and the site dialect never validates it." | SILENT | SILENT | SILENT |
| 8 | §3: "the server's own sections — the top level, a closed list; a capability's words, attached to the branch they belong to; an application's whole vocabulary, which the site's grammar does not know and does not validate." | `src/genro_asgi/config/elements.py:92-93` "site[0:1],server[0:1],middleware[0:1],authentication[0:1],storage[0:1], applications[0:1],databases[0:1],plugins[0:1],openapi[0:1]" — the closed list, each section a singleton · `tests/core/test_config.py::test_a_second_server_section_is_rejected`, `tests/core/test_config.py::test_a_section_outside_the_root_is_rejected`, `tests/core/test_config.py::test_the_envelope_attributes_are_the_apps_constructor_kwargs` | `src/genro_asgi/config/elements.py:17-22` "one ``configuration`` root … OVERRIDDEN here with the full closed section list … Every section is a singleton (``[0:1]``)"; `src/genro_asgi/config/elements.py:48-49` "``applications`` — the app collection keyed by ``code``; each entry MOUNTS the grammar its ``app_class`` carries." | SILENT | SILENT | SILENT |
| 9 | §3, elements.py:55-58 (pool clause), R11/R12 amended 2026-08-18: "a pool is not a section of the site: it belongs to the application that owns it … The same rule sends storage mounts to whoever manages volumes: this dialect declares no storage vocabulary of its own, it declares a mount point." | No pool section exists in `src/genro_asgi/config/elements.py:92-93`; the pool is read from under the application, `src/genro_asgi/config/handler.py:306-312` "def commander_kwargs(self, code: str) … the words live under ``applications.<code>.orchestration.commander``". The storage mounts are flattened generically, `src/genro_asgi/config/handler.py:203-212` "for child in node.value or (): mount = self.open_attrs(child) / mount[\"protocol\"] = child.node_tag" · `tests/core/test_config.py::test_the_section_flattens_to_genro_storage_configuration`, `tests/core/test_config.py::test_storage_without_app_is_rejected_by_the_grammar` | `src/genro_asgi/config/elements.py:54-56` "The SPA pool is NOT a section of this dialect: a pool belongs to the application that owns it, so its words live in that application's own grammar and its recipe is written under ``applications.<code>.orchestration.commander``."; `src/genro_asgi/config/elements.py:321` "This dialect declares NO storage vocabulary of its own" | `temp/cronologia_2026-08-23_2026-09-12.md` (2026-08-23) "Il pool sta sotto `application → orchestration → commander → groups`" | SILENT | SILENT |
| 10 | §3: "**Validation happens when the description is executed**, not when a value is used. A word that does not exist, a section in the wrong place, a required attribute missing, a singleton written twice: each stops the boot, naming itself." | The four failures are each pinned by a test that builds the recipe and expects the raise: `tests/core/test_config.py::test_unknown_tag_raises`, `tests/core/test_config.py::test_a_section_outside_the_root_is_rejected`, `tests/core/test_config.py::test_application_without_app_class_rejected_by_grammar`, `tests/core/test_config.py::test_a_second_server_section_is_rejected`, `tests/core/test_config.py::test_a_second_users_element_is_rejected_by_the_grammar`. The validation itself is contrib/config's, reached through `src/genro_asgi/config/builder.py:60` "class AsgiConfigBuilder(ConfigBuilder, AsgiServerGrammar):" | `src/genro_asgi/config/elements.py:20-22` "Every section is a singleton (``[0:1]``), so labels are clean and every path is stable and hand-writable" | `.subtasks/config-always/finaldoc.md` (2026-09-12) "an instance is refused by the grammar itself (`Validation failed: 'app_class': expected type`)" | SILENT | SILENT |
| 11 | §4, ratified 2026-07-29: "The same installation is described at three levels, folded lowest first: the package's defaults … the machine's layer … the site's own description — always last, always winning." | `src/genro_asgi/config/default_config.py:81-93` "parents: list[type \| Path] = [BaseConfiguration] … parents.append(self.path) … parents.append(path)" and `src/genro_asgi/asgi_server.py:173` "return ConfigurationHandler(config, parents=defaults.parents_for(config))" — the site's own recipe is the handler's top layer · `tests/core/test_config.py::test_the_defaults_layer_overrides_the_base_and_loses_to_the_site`, `tests/core/test_config.py::test_a_site_inherits_the_default_mounts_and_adds_its_key`, `tests/core/test_config.py::test_only_the_package_defaults_are_layered_without_a_defaults_recipe` | `src/genro_asgi/config/default_config.py:17-21` "Three layers reach the server's read door, lowest first: 1. ``BaseConfiguration`` — the package's shipped defaults; 2. the recipe's ``default_config`` source — the deployment's own layer; 3. the site's own recipe — always last, always winning." | `.subtasks/site-home/progress.md` (2026-09-12) "the machine defaults recipe … `<base_dir>/config.py`, layered only when the file exists … `config/default_config.py:74`" | SILENT | SILENT |
| 12 | §4: "**A recipe governs its own inheritance.** It declares where its middle layer comes from, and it may decline the layer entirely and sit straight on the package defaults. The runtime takes no argument for it: the description decides." | `src/genro_asgi/config/default_config.py:82-84` "declared = self.declared_by(source) / if declared is False: return parents" — the recipe's own `default_config` attribute decides, and `parents_for` takes no runtime argument beyond the source · `tests/core/test_config.py::test_false_refuses_the_layer_even_when_the_file_is_there`, `tests/core/test_config.py::test_unset_takes_the_conventional_file`, `tests/core/test_config.py::test_a_recipe_declining_the_layer_sees_only_the_package_defaults` | `src/genro_asgi/config/default_config.py:27-33` "WHICH source that is, the RECIPE declares (``default_config`` on ``AsgiConfigBuilder``); this class only resolves the declaration … ``False`` → no defaults layer at all: the site sits straight on the package defaults" | SILENT | SILENT | SILENT |
| 13 | §4: "**An explicit choice that cannot be honoured is an error.** A defaults file that is named and missing stops the boot; it is never a silent skip." | `src/genro_asgi/config/default_config.py:90-91` "if not path.is_file(): raise ConfigError(f\"default_config names a file that does not exist: {path}\")" · `tests/core/test_config.py::test_an_explicit_path_that_does_not_exist_is_a_config_error`, `tests/core/test_config.py::test_an_explicit_path_is_layered_from_wherever_it_lives` | `src/genro_asgi/config/default_config.py:34-36` "a path → THAT file, and a missing one is a ``ConfigError`` — an explicit choice the runtime cannot honour is a configuration mistake, never a silent skip." | SILENT | SILENT | SILENT |
| 14 | §4: "**The layering is on the tree, not on the class hierarchy.** Each recipe is executed and the results are folded" | `src/genro_asgi/config/default_config.py:81` "parents: list[type \| Path] = [BaseConfiguration]" is added for every source, whatever the source's base class, and handed to `ConfigurationHandler(config, parents=…)` at `src/genro_asgi/asgi_server.py:173` · `tests/core/test_config.py::test_a_site_inherits_the_default_mounts_and_adds_its_key` (the site recipe subclasses `AsgiConfigBuilder` and still gets the package defaults) | `src/genro_asgi/config/builder.py:137-138` "A recipe that subclasses ``AsgiConfigBuilder`` directly inherits the same defaults: the layering is the handler's, not the class hierarchy's." | SILENT | SILENT | SILENT |
| 15 | §5, ratified 2026-07-29: "A value is asked for by **path**, through one call … the written value … → the default declared by the word's own grammar … → the caller's own default → a **noisy error naming the path**." | `src/genro_asgi/config/handler.py:68` "class ConfigurationHandler(ConfigHandler):" — the stack itself is contrib/config's and is inherited whole · the four layers are pinned one by one: `tests/core/test_config.py::test_written_value_wins`, `tests/core/test_config.py::test_signature_default_is_resolved_at_read_time`, `tests/core/test_config.py::test_call_site_default_applies_to_an_unwritten_value`, `tests/core/test_config.py::test_a_missing_path_is_a_noisy_key_error` | `src/genro_asgi/config/handler.py:17-19` "it inherits the callable four-layer read stack (written value → signature default → call-site ``default=`` → noisy ``KeyError``)" | SILENT | SILENT | SILENT |
| 16 | §5: "**A part holds an address, never a copy.** An application asks with paths relative to itself and the door prefixes them with its own identity, so an application can read only its own words" | `src/genro_asgi/config/handler.py:416` "def open_attrs(self, node: Any)" and the app-side prefixing in `src/genro_asgi/application.py` — the app calls `self.config(path)` and never holds a subtree · `tests/core/test_config_env.py::test_each_app_reads_only_its_own_prefix`, `tests/core/test_config_env.py::test_written_value_in_the_mounted_subtree`, `tests/core/test_config_env.py::test_a_detached_app_raises_too` | `src/genro_asgi/application.py:59-61` "An app READS that subtree back through ``self.config(path)``, which prefixes ``applications.<code>.`` and delegates to the server's own read door: the app never holds a slice of the tree, only an address in it." | SILENT | SILENT | SILENT |
| 17 | §6, owner 2026-08-23 + D23: "The description is **not read once at boot and discarded.** It stays as a tree the running system holds: it can be read, it can be **written**, and it **notifies** whoever asked to be told (the notification is the tree's own — `Bag.subscribe`)." | The handler declares read helpers only — `src/genro_asgi/config/handler.py:71-420` is a series of `*_kwargs`/`*_config` readers with no mutator; `apply_configuration` has zero occurrences in `src/` and `tests/`, and the only `.subscribe(` in `src/` is `src/genro_asgi/applications/mcp.py:237` "queue = manager.hub.subscribe(session_id)", the MCP task hub · no test writes to the configuration tree of a running server | SILENT (`src/genro_asgi/config/handler.py:15` "ConfigurationHandler — the server's read door on its configuration." describes the read door alone) | SILENT | `f8c16f5f-78f5-495a-9eef-1d6bbcfaf77d.jsonl` (2026-08-23, assistant) "**D23** (SPECIFICATION.md:418-419) parcheggia \"the two-stage live-config architecture (config as live object, `apply_configuration`, hot/cold changes)\" come macro futura" and "Nessuno in genro-asgi lo usa oggi: `apply_configuration` non esiste e l'handler non ha mutatori." | DIVERGE |
| 18 | §6, at boot: "A description that is grammatically wrong does not run. A description that is grammatically right but **not actionable** — a mount claimed twice, an application class that will not import, a default naming something absent — **also does not run**" | `src/genro_asgi/server.py:238-241` "if app.code in self.applications: raise ValueError(f\"application code already claimed: {app.code}\") / if mount in self._by_mount: raise ValueError(f\"mount already claimed: {mount!r}\")"; the applications are instantiated at boot inside `src/genro_asgi/asgi_server.py:175-190` · `tests/core/test_config.py::test_a_default_naming_no_application_is_a_boot_error`, `tests/core/test_config.py::test_an_empty_application_code_is_a_boot_error` | `src/genro_asgi/server.py:235` "A claimed code and a claimed mount both raise ``ValueError``."; `src/genro_asgi/asgi_server.py:180-182` "``applications`` are instantiated HERE — the recipe named the classes and their kwargs, and a recipe error surfaces as a boot error instead of a broken server." | SILENT | SILENT | SILENT |
| 19 | §6: "One exception, and it is **declared by the part itself**: an application may say that a failure of its own mount is tolerable, and then the server starts without it" | SILENT (no such declaration exists: `src/genro_asgi/server.py:238-241` raises on every claimed code or mount with no tolerance flag, and no attribute of `src/genro_asgi/application.py` declares a survivable failure) | SILENT (no docstring of `application.py` or `server.py` declares a mount tolerance) | SILENT | SILENT | SILENT |
| 20 | §6, while running: "**Accepting** is atomic and immediate … the write happens **inside a boundary that encloses the notification too**: if anything raises while the change is being taken on, the description is not changed at all." | SILENT (no write path exists on the configuration tree; the handler declares no mutator) | SILENT | SILENT | SILENT | SILENT |
| 21 | §6: "**Converging** is not a transaction. Once a change is accepted, the tree holds the **state that is wanted**, and each part brings itself into line with it at its own pace" | SILENT (no convergence machinery exists in `src/`) | SILENT | SILENT | SILENT | SILENT |
| 22 | §6: "an administrator is never told \"refused\" while something was in fact taken away." | SILENT (no accept/converge path exists to report on) | SILENT | SILENT | SILENT | SILENT |
| 23 | §6, owner 2026-08-23: "Whoever declares that something of theirs can be changed while running is **guaranteeing the mechanism that makes it possible** — including undoing it." | SILENT (no part declares a hot-changeable word, so no obligation is expressed in code) | SILENT | SILENT | SILENT | SILENT |
| 24 | §6, owner 2026-08-23: "Changing a value while running is an option the design **foresees for the whole description**, and **guarantees for no part of it in advance** … a candidate placement is an attribute of the element declaration itself" | SILENT (no element declaration in `src/genro_asgi/config/elements.py` carries a hot-changeability attribute beside the type and the default) | SILENT (no declaration attribute in `src/genro_asgi/config/elements.py` expresses hot changeability) | SILENT | SILENT | SILENT |
| 25 | §7, elements.py: "The top level is a closed list of sections, each at most once, so every path is stable and can be written by hand." | `src/genro_asgi/config/elements.py:90-96` "@element(sub_tags=(\"site[0:1],server[0:1],middleware[0:1],authentication[0:1],storage[0:1],\" \"applications[0:1],databases[0:1],plugins[0:1],openapi[0:1]\"), node_label=\"configuration\",)" · `tests/core/test_config.py::test_a_second_server_section_is_rejected`, `tests/core/test_config.py::test_unknown_tag_raises` | `src/genro_asgi/config/elements.py:17-22` "one ``configuration`` root … OVERRIDDEN here with the full closed section list … Every section is a singleton (``[0:1]``), so … every path is stable and hand-writable" | SILENT | SILENT | SILENT |
| 26 | §7: "The **listener** — where to bind — and the **public address** … are two different words … The public address is **declared, not derived from a request** … A provider that needs it and does not find it is a boot error" | Two separate words on the `server` element, and the boot check: `src/genro_asgi/asgi_server.py:132-136` "self._config_host … self._config_port … external_url: str \| None = kwargs.pop(\"external_url\", None)" and `src/genro_asgi/asgi_server.py:244-250` "if providers and self.external_url is None: … raise ValueError(f\"oidc provider(s) {codes} configured but the server has no external_url…\")" · `tests/server_app/test_oidc.py::test_a_configured_provider_without_external_url_refuses_to_boot`, `tests/core/test_default_configuration.py::test_the_public_address_reaches_the_tree`, `tests/core/test_default_configuration.py::test_the_listener_kwargs_reach_the_tree` | `src/genro_asgi/asgi_server.py:52-60` "``host``/``port`` are the LISTENER; ``external_url`` is the server's PUBLIC base address … it is DECLARED rather than derived from a request … Missing it with a provider configured is a boot error (``_check_oidc_external_url``), not an opaque provider error at the first login." | SILENT | SILENT | SILENT |
| 27 | §7: "Its children are the words that are server-domain rather than application-domain: how long a session lives, and the task backbone's own vocabulary, declared by the task backbone." | The `server` element has THREE children in the code, not two: `src/genro_asgi/config/elements.py:170-175` "@element(parent_tags=\"server\", sub_tags=\"\") / def websocket(self, origins: str \| BagResolver = None, max_concurrent: int \| BagResolver = None,)" beside `session` and `tasks` · `tests/core/test_default_configuration.py::test_the_handshake_options_reach_the_tree`, `tests/core/test_config.py::test_session_child_reaches_the_store_ttl`, `tests/core/test_config.py::test_tuning_reaches_scheduler_and_store` | `src/genro_asgi/config/elements.py:38-41` "``server`` — the runtime options (``host``, ``port``, ``external_url``, ``max_threads``, ``shutdown_timeout_seconds``) plus the server-domain children ``session`` (the session TTL) and ``tasks`` (declared by ``TaskGrammar``, the class that peels ``tasks=``)."; `src/genro_asgi/config/elements.py:166-167` names all three: "Children are server-domain: ``session`` (the session TTL), ``tasks`` (the task backbone, declared by ``TaskGrammar``) and ``websocket``." | SILENT | SILENT | DIVERGE |
| 28 | §7: "The other sections — middleware, authentication, storage, applications, databases, plugins — are named at the top level and described by the entry that owns each." | `src/genro_asgi/config/elements.py:92-93` names those six plus `site` and `openapi` · `tests/core/test_config.py::test_openapi_and_databases_boot_without_error` | `src/genro_asgi/config/elements.py:42-52` lists "``middleware``", "``authentication``", "``storage``", "``applications``", "``databases``", "``plugins``", "``openapi``" | SILENT | SILENT | SILENT |
| 29 | §8: "**Not a settings file.** No free-form keys, no untyped values, no section nobody declared." | The `middleware` and `plugins` elements take free keys by design: `src/genro_asgi/config/elements.py:221` "**extra: bool \| dict," on `middleware`, and `src/genro_asgi/config/elements.py:408` "def plugin(self, code: str = None, enabled: bool = True, **options: Any) -> None:" · `tests/core/test_default_configuration.py::test_a_switch_named_outside_the_grammar_reaches_the_tree` pins the free key as wanted behaviour | `src/genro_asgi/config/elements.py:28-30` "attributes are ANNOTATED, so their signature defaults reach the read stack of ``ConfigurationHandler`` (an unannotated parameter never enters ``call_args_validations``)". The `middleware` signature is stated open: `src/genro_asgi/config/elements.py:228-229` "the signature is OPEN so that the switches have ONE place" | `.subtasks/config-always/finaldoc.md` (2026-09-12) "`middleware(...)` / `plugins(...)` gain `**kwargs`; the constructor kwargs disappear into the tree" | SILENT | DIVERGE |
| 30 | §8: "**Not a service registry.** The description says what is installed, not where to find something at runtime." | SILENT (no lookup surface exists on the handler: `src/genro_asgi/config/handler.py:71-420` returns constructor kwargs only) | SILENT | SILENT | SILENT | SILENT |
| 31 | §8: "**Not a secret store.** It names where a secret comes from; it never holds one." | The resolver is resolved at read time, never stored: `src/genro_asgi/config/handler.py:212` "return mounts, self(\"storage.storage_key\", default=None)" reads through the stack at the moment of the call · `tests/core/test_config_env.py::test_the_environment_is_read_not_frozen_into_the_recipe`, `tests/core/test_config_env.py::test_resolved_storage_key_arms_encryption_end_to_end`, `tests/core/test_config_env.py::test_a_storage_key_resolving_empty_is_a_boot_error` | `src/genro_asgi/config/elements.py:333-335` "Give it a resolver so the secret stays out of the recipe — a resolver, never a lambda, since a callback does not serialize" | `.subtasks/server_application_split/progress.md` (2026-09-12) "**A resolver nested in a dict kwarg is never resolved.**" | SILENT | SILENT |
| 32 | friction (follow-up 2026-09-08): "The general live-tree target remains undelivered. SPA group-profile hot apply is a narrower implemented mechanism, not a replacement for that target." | `apply_configuration` has zero occurrences in `src/` and `tests/`; the narrower mechanism exists on the SPA side, read through `src/genro_asgi/config/handler.py:346` "def group_kwargs(self, code: str) -> dict[str, dict[str, Any]]:" · `tests/core/test_orchestration_profile_store.py` covers the group profiles; no test touches a general live tree | SILENT (no docstring of `src/genro_asgi/config/` describes a live tree) | `temp/cronologia_2026-08-23_2026-09-12.md` (2026-08-23) "Hot apply `apply_group_settings` in tre stadi; route `_orchestration` (apply/reload/status) solo con gate." | `f8c16f5f-78f5-495a-9eef-1d6bbcfaf77d.jsonl` (2026-08-23, assistant) "`apply_configuration` non esiste e l'handler non ha mutatori" | SILENT |
| 33 | friction (follow-up 2026-09-08): "The five complete recipes are now checked by `tests/test_documentation_recipes.py`" | The test exists and is parametrized over the recipes it collects: `tests/test_documentation_recipes.py:22-35` "for path in sorted((self.root / \"internals\").rglob(\"design.md\")): … assert len(blocks) == 1, f\"{path}: expected one complete Python recipe\"" · `tests/test_documentation_recipes.py::test_complete_configuration_constructs`. Five `design.md` files carry the section at commit `9ebfa56`: `internals/10_server/010_server`, `015_configuration`, `020_applications`, `025_routing-system`, `030_middleware` | SILENT (not a docstring matter; `tests/test_documentation_recipes.py:1-4` states "Contract: complete internals recipes construct a server in isolation. This checks construction, not lifespan startup or HTTP behavior.") | SILENT | SILENT | SILENT |
| 34 | friction (follow-up 2026-09-08): "`BaseConfiguration` hooks and optional storage-key behavior are described in current status; omission of key material is distinct from an empty resolver." | `src/genro_asgi/config/builder.py:141` "storage_key: str \| BagResolver \| None = None" — the absent key is `None`, while an empty resolved value is refused elsewhere; `src/genro_asgi/config/handler.py:204-212` yields an empty mount list rather than an error for a key-only section · `tests/core/test_config.py::test_a_key_only_section_without_parents_yields_no_mount`, `tests/core/test_config_env.py::test_a_storage_key_resolving_empty_is_a_boot_error` | `src/genro_asgi/config/builder.py:123-126` "A site deviates by overriding ONE hook and nothing else — ``storage_key`` for the key material, ``storage_mounts`` for the layout, ``server_section`` for the listener"; `src/genro_asgi/config/handler.py:197-199` "A section carrying only its ``storage_key`` and no mount is legitimate" | SILENT | SILENT | SILENT |
| 36 | friction S2 [live-config]: "where the capability is declared. §6 names one candidate — an attribute of the element declaration, next to the word's type and default — and deliberately leaves it unsettled." | SILENT (no element declaration carries such an attribute) | SILENT | SILENT | SILENT | SILENT |
| 37 | friction S3 [live-config]: "a convergence that does not finish. … Wait indefinitely, force after a declared grace period, or give up and revert: not decided" | SILENT (no convergence wait exists in `src/`) | SILENT | `temp/liturgia_riavvio_orientamenti_2026-08-20.md` (2026-08-20) — the file `decisions.md` cites exists; no line of it decides the question | SILENT | SILENT |
| 38 | friction S4 [live-config]: "a visible convergence state. §6 distinguishes *accepted and in progress* from *done*, and nothing in the system can currently express that difference." | SILENT (no state of that kind exists in `src/`) | SILENT | SILENT | SILENT | SILENT |
| 39 | friction S5 [unread]: "`openapi` is grammar with no reader. The section is declared and validated by the grammar (elements.py:362) and **no code consumes it**: the only other mention in the package is the handler's own docstring saying so (handler.py:49)." | `src/genro_asgi/config/elements.py:413-422` declares the element and `src/genro_asgi/config/handler.py:71-420` holds no `openapi` reader — there is no `openapi_config` helper beside `middleware_config` and `plugins_config` · `tests/core/test_config.py::test_openapi_and_databases_boot_without_error` asserts the section is accepted and nothing consumes it | `src/genro_asgi/config/elements.py:420-422` "``openapi`` … Grammar only in core 1a — the OpenAPI application arrives in core 1c; read and skipped here."; `src/genro_asgi/config/handler.py:48` "``openapi`` → no core-1a consumer; read and skipped." The line `decisions.md` cites (elements.py:362) is not the `openapi` element, which at commit `9ebfa56` is at `elements.py:414` | SILENT | SILENT | SILENT |
| 40 | friction S6 [unread]: "the middleware switches name a closed registry. Only the core's own middleware can be configured; one registered from outside cannot be." | `src/genro_asgi/config/elements.py:221` "**extra: bool \| dict," — the signature is open, so a middleware registered from outside is written by its own name · `tests/core/test_default_configuration.py::test_a_switch_named_outside_the_grammar_reaches_the_tree` pins exactly the case the friction says is impossible | `src/genro_asgi/config/elements.py:226-229` "a middleware registered from outside (``middleware_registry=``) is written by its own name and rides through ``**extra`` — the signature is OPEN so that the switches have ONE place, the configuration, whatever registry the class came from." | `.subtasks/config-always/finaldoc.md` (2026-09-12) "Pinned: a `stamp` middleware registered through `middleware_registry` has its switch in the tree" | SILENT | DIVERGE |
| 41 | friction S7 [untested]: "the recipe in this page must stay executable. … The test that executes all of them is decided and deferred (owner, 2026-08-23), so until it exists these recipes are kept honest by hand." | The test exists at commit `9ebfa56`: `tests/test_documentation_recipes.py:39` "def test_complete_configuration_constructs(path, code, tmp_path):", which writes each collected recipe to a file and runs it in a subprocess · `tests/test_documentation_recipes.py::test_complete_configuration_constructs` | SILENT (not a docstring matter) | SILENT | SILENT | DIVERGE |
| 42 | friction S9 [undocumented]: "this entry never documents `BaseConfiguration`'s hooks. A site recipe deviates from the package defaults by overriding one hook — `server_section`, `storage_section`, or the `storage_mounts` that the second calls." | The three hooks are methods of the shipped recipe: `src/genro_asgi/config/builder.py:151` "def server_section(self, cfg: Any) -> None:", `src/genro_asgi/config/builder.py:161` "def storage_section(self, cfg: Any) -> None:", `src/genro_asgi/config/builder.py:95` "def storage_mounts(self, section: Any) -> None:", and `storage_section` calls `storage_mounts` at `src/genro_asgi/config/builder.py:163` · `tests/core/test_config.py::test_a_site_inherits_the_default_mounts_and_adds_its_key`, `tests/core/test_default_configuration.py::test_the_template_brings_the_shipped_storage_layout` | `src/genro_asgi/config/builder.py:123-126` "``BaseConfiguration`` … A site deviates by overriding ONE hook and nothing else — ``storage_key`` for the key material, ``storage_mounts`` for the layout, ``server_section`` for the listener"; `src/genro_asgi/config/builder.py:162` "The ``storage`` section: genro-storage's mount point plus the key material." | `.subtasks/site-home/finaldoc.md` (2026-09-12) "`BaseConfiguration.storage_mounts` moved up to `AsgiConfigBuilder` and writes **two**…" | SILENT | SILENT |
| 43 | friction S10 [undocumented]: "the recipe reads as if the storage key were required. … the default is `None` and absence is not the same as a resolver that came back empty." | `src/genro_asgi/config/builder.py:141` "storage_key: str \| BagResolver \| None = None" — the default is `None`, and a section without a key boots · `tests/core/test_config.py::test_a_key_only_section_without_parents_yields_no_mount`, `tests/core/test_config_env.py::test_a_storage_key_resolving_empty_is_a_boot_error` — the two cases are distinguished by two tests | `src/genro_asgi/config/builder.py:142` "At-rest key material of the storage section — a site sets it to a resolver."; `src/genro_asgi/config/handler.py:197-201` "A section carrying only its ``storage_key`` and no mount is legitimate" | SILENT | SILENT | SILENT |

## Divergences

### #1
- decisions.md says: "One configuration is the whole site; each process materializes the projection of its own role."
- the implementation is silent: `src/genro_asgi/asgi_server.py:127` builds one handler per server over the whole description, and no code projects it per process role.
- no docstring speaks of it.
- no handoff or finaldoc speaks of it.
- no transcript speaks of it.

### #3
- decisions.md says: "there is no builder, no factory, no assembler that knows the whole system."
- the implementation does: `src/genro_asgi/asgi_server.py:175-196` reads one section per helper and hands each kwarg to the class that peels it.
- the docstring says: `src/genro_asgi/config/handler.py:20-22` "there is one direction of dependency and no materializer."
- no handoff or finaldoc speaks of it.
- no transcript speaks of it.

### #5
- decisions.md says: "The recipe orchestrates; the sections describe."
- the implementation does: `src/genro_asgi/config/builder.py:144-149` writes the shipped recipe that way.
- the docstring says: `src/genro_asgi/config/builder.py:23-24` "Recipes orchestrate in ``main`` and delegate each section to a method taking the PARENT node".
- no handoff or finaldoc speaks of it.
- no transcript speaks of it.

### #6
- decisions.md says: "Words whose only legitimate source is outside **refuse a literal at the recipe line**: the bootstrap administrator password cannot be typed into a description at all."
- the implementation does: no word of the grammar refuses a literal. The annotations are `<type> | BagResolver` and take either (`src/genro_asgi/config/elements.py:286` "password: str | BagResolver = None", `src/genro_asgi/config/elements.py:318` "storage_key: str | BagResolver = None"). The bootstrap password word was removed instead, and the test that fixes it is `tests/core/test_config.py::test_the_bootstrap_password_is_no_longer_a_word`.
- the docstring says: `src/genro_asgi/config/elements.py:244-245` "The server creates no user either (owner, 2026-09-12): there is no bootstrap password word."
- the progress of 2026-09-12 (`.subtasks/config-always/progress.md`) says: "The grammar element `authentication.admin_password(node_value: BagResolver = None)` REFUSES a literal by a ratified decision"; the finaldoc of the same subtask records commit `ddd7d2c` "feat: the server creates no user, and the session snapshot is a configuration word", which removes the word.
- no transcript speaks of it.

### #7
- decisions.md says: "Each part declares the words it consumes, next to the code that reads them".
- the implementation does: the `application` and `storage` elements mount a foreign grammar by reference (`src/genro_asgi/config/elements.py:317`, `src/genro_asgi/config/elements.py:357`).
- the docstring says: `src/genro_asgi/application.py:52-56` "the app declares its own vocabulary and the site dialect never validates it."
- no handoff or finaldoc speaks of it.
- no transcript speaks of it.

### #17
- decisions.md says: "It stays as a tree the running system holds: it can be read, it can be **written**, and it **notifies** whoever asked to be told (the notification is the tree's own — `Bag.subscribe`)."
- the implementation does: `src/genro_asgi/config/handler.py:71-420` declares readers only, `apply_configuration` has zero occurrences in `src/` and `tests/`, and the only `.subscribe(` in `src/` is `src/genro_asgi/applications/mcp.py:237`, the MCP task hub.
- the docstring says: `src/genro_asgi/config/handler.py:15` "ConfigurationHandler — the server's read door on its configuration." No mutator is documented.
- no handoff or finaldoc speaks of it.
- the transcript `f8c16f5f-78f5-495a-9eef-1d6bbcfaf77d.jsonl` of 2026-08-23 (assistant) says: "Nessuno in genro-asgi lo usa oggi: `apply_configuration` non esiste e l'handler non ha mutatori."

### #19
- decisions.md says: "an application may say that a failure of its own mount is tolerable, and then the server starts without it rather than refusing to start at all."
- the implementation is silent: `src/genro_asgi/server.py:238-241` raises `ValueError` on a claimed code or mount with no tolerance flag, and no attribute of `src/genro_asgi/application.py` declares a survivable failure.
- no docstring speaks of it.
- no handoff or finaldoc speaks of it.
- no transcript speaks of it.

### #20, #21, #22, #23, #24
- decisions.md says (§6, while running): "**Accepting** is atomic and immediate", "**Converging** is not a transaction", "an administrator is never told \"refused\" while something was in fact taken away", "Whoever declares that something of theirs can be changed while running is **guaranteeing the mechanism that makes it possible**", "a candidate placement is an attribute of the element declaration itself".
- the implementation is silent: no write path, no convergence machinery and no hot-changeability attribute exist in `src/`.
- no docstring speaks of them.
- no handoff or finaldoc speaks of them.
- no transcript speaks of them beyond the parking recorded in D23 (row #17).

### #27
- decisions.md says: "Its children are the words that are server-domain rather than application-domain: how long a session lives, and the task backbone's own vocabulary, declared by the task backbone."
- the implementation does: the `server` element declares a third child, `src/genro_asgi/config/elements.py:170-175` "def websocket(self, origins: str | BagResolver = None, max_concurrent: int | BagResolver = None,)", which `decisions.md` does not name; it is pinned by `tests/core/test_default_configuration.py::test_the_handshake_options_reach_the_tree`.
- the docstring says: `src/genro_asgi/config/elements.py:38-41` names the same two children as `decisions.md`, while `src/genro_asgi/config/elements.py:166-167` names all three: "``session`` (the session TTL), ``tasks`` (the task backbone, declared by ``TaskGrammar``) and ``websocket``."
- no handoff or finaldoc speaks of it.
- no transcript speaks of it.

### #29
- decisions.md says: "**Not a settings file.** No free-form keys, no untyped values, no section nobody declared."
- the implementation does: `src/genro_asgi/config/elements.py:221` "**extra: bool | dict," and `src/genro_asgi/config/elements.py:408` "def plugin(self, code: str = None, enabled: bool = True, **options: Any) -> None:" accept undeclared keys, and `tests/core/test_default_configuration.py::test_a_switch_named_outside_the_grammar_reaches_the_tree` pins that as wanted behaviour.
- the docstring says: `src/genro_asgi/config/elements.py:228-229` "the signature is OPEN so that the switches have ONE place, the configuration, whatever registry the class came from".
- the finaldoc of 2026-09-12 (`.subtasks/config-always/finaldoc.md`) says: "`middleware(...)` / `plugins(...)` gain `**kwargs`; the constructor kwargs disappear into the tree".
- no transcript speaks of it.

### #30
- decisions.md says: "**Not a service registry.** The description says what is installed, not where to find something at runtime."
- the implementation is silent: `src/genro_asgi/config/handler.py:71-420` returns constructor kwargs only, with no lookup surface.
- no docstring speaks of it.
- no handoff or finaldoc speaks of it.
- no transcript speaks of it.

### #32
- decisions.md says: "SPA group-profile hot apply is a narrower implemented mechanism, not a replacement for that target."
- the implementation does: the narrower mechanism is read through `src/genro_asgi/config/handler.py:346` "def group_kwargs(self, code: str) -> dict[str, dict[str, Any]]:", while `apply_configuration` has zero occurrences.
- no docstring of `src/genro_asgi/config/` describes a live tree.
- the handoff of 2026-08-23 (`temp/cronologia_2026-08-23_2026-09-12.md`) says: "Hot apply `apply_group_settings` in tre stadi; route `_orchestration` (apply/reload/status) solo con gate."
- the transcript `f8c16f5f-78f5-495a-9eef-1d6bbcfaf77d.jsonl` of 2026-08-23 (assistant) says: "`apply_configuration` non esiste e l'handler non ha mutatori".

### #33
- decisions.md says: "The five complete recipes are now checked by `tests/test_documentation_recipes.py`".
- the implementation does: `tests/test_documentation_recipes.py:22-35` collects one recipe per `design.md` carrying the section, and five files carry it at commit `9ebfa56`.
- the docstring of the test file says: `tests/test_documentation_recipes.py:1-4` "This checks construction, not lifespan startup or HTTP behavior."
- no handoff or finaldoc speaks of it.
- no transcript speaks of it.

### #36, #37, #38
- decisions.md says (frictions S2, S3, S4): "where the capability is declared", "a convergence that does not finish", "a visible convergence state".
- the implementation is silent: none of the three mechanisms exists in `src/`.
- no docstring speaks of them.
- for S3 the file `temp/liturgia_riavvio_orientamenti_2026-08-20.md` cited by `decisions.md` exists, and no line of it decides the question; no handoff speaks of S2 or S4.
- no transcript speaks of them.

### #39
- decisions.md says: "`openapi` is grammar with no reader. The section is declared and validated by the grammar (elements.py:362) and **no code consumes it**".
- the implementation does: `src/genro_asgi/config/elements.py:413-422` declares the element and no reader exists in `src/genro_asgi/config/handler.py`; `tests/core/test_config.py::test_openapi_and_databases_boot_without_error` asserts the section is accepted and unread.
- the docstring says: `src/genro_asgi/config/handler.py:48` "``openapi`` → no core-1a consumer; read and skipped."
- no handoff or finaldoc speaks of it.
- no transcript speaks of it.

### #40
- decisions.md says: "Only the core's own middleware can be configured; one registered from outside cannot be."
- the implementation does: `src/genro_asgi/config/elements.py:221` "**extra: bool | dict," admits a name from any registry, and `tests/core/test_default_configuration.py::test_a_switch_named_outside_the_grammar_reaches_the_tree` pins exactly the case the friction says is impossible.
- the docstring says: `src/genro_asgi/config/elements.py:226-229` "a middleware registered from outside (``middleware_registry=``) is written by its own name and rides through ``**extra``".
- the finaldoc of 2026-09-12 (`.subtasks/config-always/finaldoc.md`) says: "Pinned: a `stamp` middleware registered through `middleware_registry` has its switch in the tree".
- no transcript speaks of it.

### #41
- decisions.md says: "The test that executes all of them is decided and deferred (owner, 2026-08-23), so until it exists these recipes are kept honest by hand."
- the implementation does: `tests/test_documentation_recipes.py:39` "def test_complete_configuration_constructs(path, code, tmp_path):" exists at commit `9ebfa56` and runs every collected recipe in a subprocess. The 2026-09-08 follow-up of the same `decisions.md` records it ("The five complete recipes are now checked by `tests/test_documentation_recipes.py`"), and the historical text of the friction was not corrected.
- no docstring speaks of it.
- no handoff or finaldoc speaks of it.
- no transcript speaks of it.

### #43
- decisions.md says: "the default is `None` and absence is not the same as a resolver that came back empty."
- the implementation does: `src/genro_asgi/config/builder.py:141` "storage_key: str | BagResolver | None = None", and the two cases are separated by `tests/core/test_config.py::test_a_key_only_section_without_parents_yields_no_mount` and `tests/core/test_config_env.py::test_a_storage_key_resolving_empty_is_a_boot_error`.
- the docstring says: `src/genro_asgi/config/handler.py:197-201` "A section carrying only its ``storage_key`` and no mount is legitimate".
- no handoff or finaldoc speaks of it.
- no transcript speaks of it.

(The remaining `SILENT` rows — #2, #4, #8, #9, #10, #11, #12, #13, #14, #15, #16, #18, #25, #26, #28, #31, #34, #42 — are `SILENT` for one reason only: no transcript speaks of them, and for some no handoff does either. Their implementation, docstring and, where present, handoff say the same thing; the quotes are in the table.)

