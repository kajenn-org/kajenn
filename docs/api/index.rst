API Reference
=============

The API reference is generated from the source docstrings. The pages below group the core and the
base server application by subsystem; private implementation helpers are not an API guarantee.

The multiworker orchestration API is documented in ``kajenn-orchestra``, the Django adapter in
``kajenn-django``.

.. toctree::
   :maxdepth: 2

   core
   applications
   auth
   session
   middleware
   tasks
   config
   channel
   storage
   plugins
   websocket
   server-app
